export interface CheckItem {
  orderItemId: string;
  visitorId: string;
  visitor: string;
  menuItemId: string;
  menuItem: string;
  menuItemSizeId: string;
  menuItemSize: string;
  price: number;
}
